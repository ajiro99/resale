h5 売上一覧

= render 'search_form'

= link_to '登録', new_sale_path, class: 'button'

table
  tbody
    tr
      td
      td
      td
      td.text-right = "合計　#{@sales_total.size.to_s(:delimited)}個"
      td.text-right =@sales_total.total_stocking_price
      td.text-right =@sales_total.total_bonus_price
      td.text-right.red_text =@sales_total.total_cost
      td.text-right =@sales_total.total_selling_price
      td.text-right =@sales_total.total_fee
      td.text-right =@sales_total.total_shipping_cost
      td.text-right.red_text =@sales_total.total_sales
      td.text-right.red_text =@sales_total.total_profit
      td.text-right =@sales_total.total_profit_rate
      td
      td

    tr
      th = sort_link(@q, :sales_date)
      th = sort_link(@q, :product_type)
      th = sort_link(@q, :stocking_products_product_maker, 'メーカー')
      th = sort_link(@q, :stocking_products_product_name, '商品名')
      th = sort_link(@q, :stocking_price, default_order: :desc)
      th = sort_link(@q, :bonus_price, default_order: :desc)
      th = sort_link(@q, :cost, default_order: :desc)
      th = sort_link(@q, :selling_price, default_order: :desc)
      th = sort_link(@q, :fee, default_order: :desc)
      th = sort_link(@q, :shipping_cost, default_order: :desc)
      th = sort_link(@q, :sales, default_order: :desc)
      th = sort_link(@q, :profit, default_order: :desc)
      th = sort_link(@q, :profit_rate, default_order: :desc)
      th
      th

  tbody
    - @sales.each do |sale|
      tr
        td = date_text(sale.sales_date)
        td = sale.product_type_text
        td = sale.maker
        td == sale.product_name
        td.text-right = sale.stocking_price.to_s(:delimited)
        td.text-right = sale.bonus_price.to_s(:delimited)
        td.text-right.red_text = sale.cost.to_s(:delimited)
        td.text-right = sale.selling_price.to_s(:delimited)
        td.text-right = sale.fee.to_s(:delimited)
        td.text-right = sale.shipping_cost.to_s(:delimited)
        td.text-right.red_text = sale.sales.to_s(:delimited)
        td.text-right.red_text = sale.profit.to_s(:delimited)
        td.text-right = ActionController::Base.helpers.number_to_percentage(sale.profit_rate.to_s(:delimited), precision: 2)
        = render partial: 'parts/edit_delete_link', locals: {edit_path: edit_sale_path(sale), delete_path: sale}

= paginate @sales_q
